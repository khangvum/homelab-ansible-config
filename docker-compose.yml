services:
  ansible_service:
    restart: always
    container_name: ansible_service
    build:
      context: ./docker-settings
      dockerfile: Dockerfile
    volumes:
      - ./ansible:/opt/ansible
    environment:
      - ANSIBLE_CONFIG=/opt/ansible/ansible.cfg